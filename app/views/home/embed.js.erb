(function(){
  document.write(
    '<iframe style="border:none;" id="map" width="<%=@width%>" height="<%=@height%>" src="<%=raw embed_url(:width => @width, :height => @height, :mode => @mode) %>"></iframe>'
  );
})();
